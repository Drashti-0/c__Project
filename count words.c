//space remove

#include<stdio.h>
#include<string.h>

int main(){

int a[200],b[200];
printf("Enter your string");
scanf("%s",&a[200]);
int j=0;
for(int i=0;a[i]!='\0';i++){
        if(a[i]==' '){
          b[i]==a[i];
          j++;
        }
    b[j]='\0';
}
printf("%s",b);























}
